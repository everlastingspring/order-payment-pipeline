spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/notification_db
    username: ${DB_USERNAME:popuser}
    password: ${DB_PASSWORD:poppass}

  kafka:
    bootstrap-servers: kafka:9092

management:
  otlp:
    tracing:
      endpoint: http://otel-collector:4318/v1/traces
